var React = require('react'),
    ReactDOM = require('react-dom'),
    PhotoHub = require('./photo-hub'),
    axios = require('axios');

ReactDOM.render(
  <PhotoHub />,
  document.getElementById('photo-hub')
);
